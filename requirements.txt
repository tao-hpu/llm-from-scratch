# 全仓依赖。装这些就能跑通全部 9 个脚本:
# bigram → attention → transformer → GPT-2 124M 预训练/推理 → SFT / LoRA / DPO → MoE。
# 建议 Python 3.10+。装 torch 请按你的平台到 https://pytorch.org 选对应命令：
#   - NVIDIA 显卡(Windows/Linux)：选 CUDA 版
#   - Mac(Apple Silicon)：默认 pip 版即自带 MPS
#   - 纯 CPU：默认 pip 版
torch>=2.2
numpy
tiktoken          # GPT-2 真 BPE 分词（phase1-124m / phase2-sft-lora 用）
pyarrow           # 读本地 FineWeb-Edu parquet（只有 phase1-124m 的数据预处理用）
# huggingface_hub  # 可选：下载预训练好的权重时用
